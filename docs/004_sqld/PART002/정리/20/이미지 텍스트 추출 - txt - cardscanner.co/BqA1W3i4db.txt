
45
다음 중아래 TAB1을 보고 각 SQL 실행 결과를 가장 올바르게 설명한 것을 고르시오.
[TAB1]
COL1
а
b
C
d
e
COL2
NULL
1Y
3
4
3
아래
46
1 SELECT COL2 FROM TABI WHERE COLI='b';
←
실행 결과가 없다. (공집합)
SELECT ISNULL(COL2, 'X') FROM TAB1 WHERE COLI = '';
실행 결과로 'X'를 반환한다.
3 SELECT COUNT(COL1) FROM TAB1 WHERE COL2= NULL; 실행 결과는 1이다.
SELECT COUNT(COL2) FROM TAB1 WHERE COLI IN ('b',''); 실행 결과는 1이다.
사원 테이블에서 MGR의 값이 7698과 같으면 NULL을 표시하고, 같지 않으면 MGR을 표시 하려고 한다. 아래 SQL 문장의 ᄀ 안에 들어갈 함수명을 작성하시오.
아래
SELECT ENAME, EMPNO, MGR,
(MGR,7698) as NM
FROM EMP;