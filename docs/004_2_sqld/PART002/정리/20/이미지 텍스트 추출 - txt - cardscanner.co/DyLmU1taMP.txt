
48
47
다음 중 아래 데이터를 가지고 있는 EMP_Q 테이블에서 세개의 SQL
결과로 가장 적절한 것은?
아래
SELECT SAL/COMM FROM EMP_Q WHERE ENAME='KING: SELECT SAL/COMM FROM EMP_Q WHERE ENAME
'FORD';
SELECT SAL/COMM FROM EMP_Q WHERE ENAME='SCOTT:
※ 단, SCOTT의 COMM은 NULL 값임
EMP_Q
ENAME (문자타입)
SAL (숫자타임)
COMM (숫자타입)
KING
0
300
FORD
5000
0
SCOTT
1000
1 0, NULL, NULL
9 0. 에러 발생, 에러 발생
9 에러 발생. 에러 발생, NULL
4 0. 에러 발생, NULL
다음 중 아래와 같은 데이터 상황에서 SQL의 수행 결과로 가장 적절한 것은?
TAB1
C1
C2
C3
1
2
3
2
3
3
0
1
3) 6
4 14
아래
SELECT SUM(COALESCE
(C1, C2, C3))
FROM TAB1